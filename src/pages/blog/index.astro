---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { formatDate } from '../../utils/date';

const posts = (await getCollection('blog'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

---

<BaseLayout title="Blog" description="Essays, notes, project logs, and field reports.">
  <section>
    <p class="eyebrow">Blog</p>
    <h1 class="page-title">Field notes.</h1>
    <p class="lead">
      Essays on agent harnesses, context engineering, and spec-driven development. New posts arrive
      whenever something is worth writing down.
    </p>
  </section>

  <section class="section">
    <div class="article-list">
      {
        posts.map((post) => (
          <a class="article-link" href={`/blog/${post.id}/`}>
            <time datetime={post.data.pubDate.toISOString()}>{formatDate(post.data.pubDate)}</time>
            <span>
              <h2>{post.data.title}</h2>
              <p>{post.data.description}</p>
              <span class="tag-list">
                {post.data.tags.map((tag) => <span class="tag">{tag}</span>)}
              </span>
            </span>
          </a>
        ))
      }
    </div>
  </section>
</BaseLayout>
