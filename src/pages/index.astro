---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import { interests, nowItems, profile, projects, site } from '../data/site';
import { formatDate } from '../utils/date';

const posts = (await getCollection('blog'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);
---

<BaseLayout title={site.title} description={site.description}>
  <section class="hero">
    <div>
      <p class="eyebrow">Personal field notebook</p>
      <h1>Tianchi keeps notes.</h1>
      <p class="lead">{profile.intro}</p>
      <div class="tag-list" aria-label="Current focus areas">
        {profile.focusAreas.map((area) => <span class="tag">{area}</span>)}
      </div>
    </div>
    <aside class="panel">
      <p class="eyebrow is-secondary">Current signal</p>
      <h2>{profile.headline}</h2>
      <p>{profile.location}</p>
      <ul>
        {nowItems.map((item) => <li>{item}</li>)}
      </ul>
    </aside>
  </section>

  <section class="section">
    <div class="section-heading">
      <div>
        <span class="section-badge" aria-hidden="true">A</span>
        <div>
          <p class="eyebrow">Projects</p>
          <h2>Things in motion</h2>
        </div>
      </div>
      <p>Selected projects and experiments. Replace these with your real portfolio as it evolves.</p>
    </div>
    <div class="grid">
      {
        projects.map((project) => (
          <article class="card">
            <p class="eyebrow is-muted">{project.status}</p>
            <h3>{project.name}</h3>
            <p>{project.description}</p>
            <div class="tag-list">
              {project.tags.map((tag) => <span class="tag">{tag}</span>)}
            </div>
          </article>
        ))
      }
    </div>
  </section>

  <section class="section">
    <div class="section-heading">
      <div>
        <span class="section-badge is-secondary" aria-hidden="true">B</span>
        <div>
          <p class="eyebrow is-secondary">Notebook</p>
          <h2>Latest writing</h2>
        </div>
      </div>
      <a href="/blog/">All posts →</a>
    </div>
    <div class="article-list">
      {
        posts.map((post) => (
          <a class="article-link" href={`/blog/${post.id}/`}>
            <time datetime={post.data.pubDate.toISOString()}>{formatDate(post.data.pubDate)}</time>
            <span>
              <h3>{post.data.title}</h3>
              <p>{post.data.description}</p>
            </span>
          </a>
        ))
      }
    </div>
  </section>

  <section class="section">
    <div class="section-heading">
      <div>
        <span class="section-badge is-mint" aria-hidden="true">C</span>
        <div>
          <p class="eyebrow is-mint">Interests</p>
          <h2>Recurring questions</h2>
        </div>
      </div>
    </div>
    <div class="grid two">
      {interests.map((item) => <article class="card"><h3>{item}</h3></article>)}
    </div>
  </section>
</BaseLayout>
